### Параметры для команды git brunch

- git brunch
Отображение списка веток в репозитории. Это синоним команды git branch --list.

- git brunch "название ветки"
*не переключает на эту ветку автоматически

- git brunch -d "название ветки"
Удаление указанной ветки. Это «безопасная» операция, поскольку Git не позволит удалить ветку, если в ней есть неслитые изменения.

- git brunch -D "название ветки"
Принудительное удаление указанной ветки, даже если в ней есть неслитые изменения. 

- git brunch -m "новое название ветки"
Изменение имени текущей ветки.

- git brunch -a
Вывод списка всех удаленных веток.
